# Exemple de script .lambda avec commentaires et semi-colons
# Calcul de la somme des entiers de 1 à 5 en utilisant une fonction récursive
let sum = fix (\f. \n.
    if zero n then 0
    else n + (f (n - 1))
) in sum 5 ;;
